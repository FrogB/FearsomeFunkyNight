--this script is meant to test out math.pi angle tweens, to test it out simply remove the '--' from both statements, or one. whichever one you want to test tbh

function onUpdate(elapsed)
    --setProperty('camHUD.angle',0 - 5 * math.cos((currentBeat*0.25)*math.pi) )
    --setProperty('camGame.angle',0 - 3 * math.cos((currentBeat*0.25)*math.pi) )
end